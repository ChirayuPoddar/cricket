# Game Systems

Core arcade engines live in `src/utils` and `src/stores`, while React Three Fiber entities live in `src/components/GameScene.jsx`.

This folder is reserved for expanding standalone cricket systems such as batting physics, fielder AI, delivery tuning, and match formats.
